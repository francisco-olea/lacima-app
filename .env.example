# Local PostgreSQL
DATABASE_URL=postgresql://USER:PASSWORD@HOST:5432/lacima?sslmode=disable

# In Docker, use host.docker.internal instead of localhost when PostgreSQL runs on the host.
# In production, use the real database host and set this value as a platform secret.
