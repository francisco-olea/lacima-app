services:
  myproject:
    image: myproject
    build:
      context: .
      dockerfile: ./Dockerfile
    env_file:
      - .env.local
    environment:
      NODE_ENV: production
    ports:
      - 4020:4020
